# Project-level pnpm settings live in `pnpm-workspace.yaml` (pnpm ≥ 10/11
# reads storeDir / packageImportMethod / allowBuilds from there).
# This file only keeps registry-level overrides if you need them.
